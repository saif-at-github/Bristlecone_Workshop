#ifndef HELLO_H

/* header file guard */
#define HELLO_H

/* include header files */
#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

/* function declarations */
extern void Hello(const char* str);

/* macro definitions */
#define MUL(x,y)  ((x)*(y))

#define SQR(x)      ((x)*(x))

#endif
